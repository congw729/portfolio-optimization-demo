# Minimal cloud dependencies for the Streamlit web app (app/).
# Streamlit Community Cloud installs THIS file on deploy.
# Full dev freeze (jupyter / yfinance / matplotlib / seaborn / akshare / sklearn)
# is kept in docs/requirements-full.txt.

streamlit==1.61.1
numpy==2.5.2
pandas==3.0.5
scipy==1.18.0
plotly==6.9.0
